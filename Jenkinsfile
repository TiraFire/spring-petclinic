pipeline {
  agent any
  stages {
    stage('One') {
      steps {
        echo "hello"
      }
    }
  }
  environment {
    increase = 'true'
    testpassed = 'true'
	CommitCount = 0
	Hash = ''
  }
}
